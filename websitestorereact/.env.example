# Copy to .env for local development.
# The Stripe PUBLISHABLE key is safe to expose in a browser bundle by design;
# the matching secret key belongs in the API's user-secrets, never here.
VITE_API_URL=https://localhost:5005/api
VITE_STRIPE_PK=pk_test_your_publishable_key_here
