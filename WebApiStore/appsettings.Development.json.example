{
  "_comment": "Copy to appsettings.Development.json. Leave secrets EMPTY here and set them with `dotnet user-secrets` (see README) so they are never committed.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=shop;Username=postgres;Password=Password@1"
  },
  "Cors": {
    "AllowedOrigins": [ "https://localhost:3000", "http://localhost:3000" ]
  },
  "StripeSettings": {
    "SecretKey": "",
    "PublishableKey": "",
    "WhSecret": ""
  }
}
